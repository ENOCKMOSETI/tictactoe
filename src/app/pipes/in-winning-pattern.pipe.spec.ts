import { InWinningPatternPipe } from './in-winning-pattern.pipe';

describe('InWinningPatternPipe', () => {
  it('create an instance', () => {
    const pipe = new InWinningPatternPipe();
    expect(pipe).toBeTruthy();
  });
});
